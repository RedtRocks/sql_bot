// Placeholder API module for future integrations
export function sendMessageApi(message) {
  // no-op placeholder
  return Promise.resolve({ role: 'assistant', content: `Echo: ${message}` })
}









